# python-more_classes

## Description

This project is part of the Higher Level Programming curriculum. It goes
further into Python classes and object-oriented programming, using a
`Rectangle` class to explore properties, string representations,
instance counting, class attributes, static and class methods.

## Learning Objectives

- Why Python programming is awesome
- Why object-oriented programming is awesome
- "First-class everything"
- What's a class
- What's an object and an instance
- What's the difference between a class and an object or instance
- What's an attribute
- What are and how to use public, protected, and private attributes
- What's `self`
- What's a method
- What's the special `__init__` method and how to use it
- What's `__str__` and how to use it
- What's `__repr__` and how to use it
- What's the difference between `__str__` and `__repr__`
- What's Data Abstraction, Data Encapsulation, and Information Hiding
- What's a property
- How to use the `property`, `getter`, and `setter` decorators
- What's the difference between an attribute and a property in Python
- How to dynamically create arbitrary new attributes for existing instances
  of a class
- How to bind attributes to object and classes
- What's the `__dict__` of a class and/or an instance of a class
- How does `dir()` work and how to use it
- What's the default value of `__init__`, `__str__` and `__repr__`
  of an object
- How to use the `del` keyword and what's the `__del__` method for
- What's a static method and how to define one with `@staticmethod`
- What's a class method and how to define one with `@classmethod`

## Requirements

- Editors allowed: vi, vim, emacs
- All files are interpreted on Ubuntu 20.04 LTS using python3 (version 3.8.5)
- All files end with a new line
- The first line of all files is exactly `#!/usr/bin/python3`
- Code follows the `pycodestyle` style (version 2.7.*)
- All files are executable
- No modules may be imported unless a task explicitly allows it

## Tasks

| File | Description |
| --- | --- |
| `0-rectangle.py` | Defines an empty `Rectangle` class |
| `1-rectangle.py` | `Rectangle` with `width`/`height` properties |
| `2-rectangle.py` | `Rectangle` with `area()` and `perimeter()` |
| `3-rectangle.py` | `Rectangle` with `__str__` |
| `4-rectangle.py` | `Rectangle` with `__repr__` (usable with `eval()`) |
| `5-rectangle.py` | `Rectangle` with `__del__` |
| `6-rectangle.py` | `Rectangle` with `number_of_instances` class attribute |
| `7-rectangle.py` | `Rectangle` with `print_symbol` class attribute |
| `8-rectangle.py` | `Rectangle` with `bigger_or_equal()` static method |
| `9-rectangle.py` | `Rectangle` with `square()` class method |

## Author

Created as part of the ALU Higher Level Programming curriculum.
